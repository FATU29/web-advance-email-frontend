import { SignupForm } from '@/components/authentication/sign-up-form';

export const metadata = {
  title: 'Sign Up | Email App',
  description: 'Create a new account',
};

export default function SignupPage() {
  return <SignupForm />;
}
